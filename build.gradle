buildscript {
    repositories {
        mavenLocal()
        maven { url 'https://jitpack.io' }
        gradlePluginPortal()
        mavenCentral()
    }
    dependencies {
        classpath 'com.github.UnknownDomainGames:GradlePlugin:1.2'
    }
}
apply plugin: 'pangu-gradle'

group 'com.github.unknowndomaingame'
version '1.0.0'

dependencies {
}

engine {
    version 'dev-SNAPSHOT'
}
